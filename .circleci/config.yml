version: 2.1

jobs:
  build:
    machine:
      image: ubuntu-2004:2022.07.1
    steps:
      - run: printenv DEBIAN_FRONTEND
      - run: |
          sudo apt-get update
          sudo apt-get install -y openvpn openvpn-systemd-resolved
