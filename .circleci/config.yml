version: 2.1

jobs:
  build:
    machine:
      image: ubuntu-2004:2022.07.1
    steps:
      - checkout
      - run: |
          sudo apt-get update
          sudo apt-get install -y openvpn-systemd-resolved
